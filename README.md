# relaymonkey/tap

Homebrew tap for [Relaymonkey](https://github.com/relaymonkey) CLI tools.

## rmesh

```bash
brew tap relaymonkey/tap
brew install --cask rmesh
```

Or as a one-liner:

```bash
brew install --cask relaymonkey/tap/rmesh
```
